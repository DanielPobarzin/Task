
name: Build

on:
   push
jobs:
   check-and-publish:
       name: "task-donson-build"
       runs-on: ubuntu-latest
       steps:
            - name: "Checkout source code"
              uses: actions/checkout@v2
            - name: "Setup Node.js"
              uses: actions/setup-node@v1
              with: 
                  node-version: 12
                  registry-url: https://www.npmjs.com/package/
            - name: "Edit config"
              run: git config --global user.name "daniil.nyashka@gmail.com" && git config --global user.name "Daniel"
            - name: "Version patch"
              run: npm version 0.1.$GITHUB_RUN_ID
            - name: "Install dependencies"
              run: npm install 
            - name: "Npm publish"
              run:  npm publish --loglevel verbose
              env: 
                 NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
            - name: "Telegram Notification"
              uses: "appleboy/telegram-action@master"
              with: 
                to: ${{ secrets.TELEGRAM_CHAT_ID }}
                token: ${{ secrets.TELEGRAM_TOKEN }}
                args: A new task upload to the repository has been detected. New version 0.1.${{ github.run_id }}


          
